{
  "name": "pi-extension",
  "version": "0.0.0",
  "description": "John's pi extensions",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
